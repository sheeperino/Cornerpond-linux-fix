280a281,293
>     var viewport = get_viewport()
>     var mouse_abs_pos = get_global_mouse_position() * viewport.get_screen_transform().get_scale()
>     if (mouse_abs_pos.x < 0 or mouse_abs_pos.y < 0): return
> 
>     await RenderingServer.frame_post_draw
>     var img = viewport.get_texture().get_image()
>     var pixel = viewport.get_texture().get_image().get_pixelv(mouse_abs_pos)
>     if (pixel.a < 1.0):
>         get_window().mouse_passthrough = true
>     else:
>         get_window().mouse_passthrough = false
> 
> 
